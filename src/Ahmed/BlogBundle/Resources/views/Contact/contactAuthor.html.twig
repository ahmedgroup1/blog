{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Contact Author</h1>
{{ user.email }}
    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('contact') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
